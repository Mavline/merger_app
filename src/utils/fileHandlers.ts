import { TableRow } from '../types/dataTypes';

export const processFile = async (file: File): Promise<TableRow[]> => {
  // Здесь будет логика обработки файла
  return [];
};

export const validateFile = (file: File): boolean => {
  // Здесь будет логика валидации файла
  return true;
}; 